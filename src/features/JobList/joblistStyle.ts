import {createStyles} from "@mantine/core";

export const useJoblistStyles = createStyles((theme) => ({
    wrapper:
        {
            padding: 0,
            margin: 0,
            display: "flex",
            flexDirection: "column",
            flexGrow: 1
        }
}));